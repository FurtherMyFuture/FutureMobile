/**
 * @flow
 */

import React from 'react'

import { StyleSheet, View } from 'react-native'

export function OnboardingMatchingScene(): React.Element<any> {
  return <View style={styles.container} />
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'red',
    flex: 1,
  },
})
